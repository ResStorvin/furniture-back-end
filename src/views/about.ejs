<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include("includes/head") %>
    <link rel="stylesheet" />
  </head>
  <body>
    <%- include("includes/nav") %>
    <h1><%= title %></h1>
    <ul>
      <% users.forEach(user => { %>
      <li>user- <%= user.name %> : Age-<%= user.age %></li>
      <% }) %>
    </ul>
  </body>
</html>
